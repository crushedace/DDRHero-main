using Godot;
using System;

//This should really get a better name and its own file. Currently this is so that the DirectionInputs enum can be found anywhere.
//Expand as necessary when new structs are made.
namespace Data
{
	public enum DirectionInputs 
	{
		Up = 0,
		Right = 1,
		Down = 2,
		Left = 3
		
	}
}